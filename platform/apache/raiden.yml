name: proxy
type: platform
options:
    expose:
        - "80:80"
    mount:
        - "pool:/etc/apache2/conf.d"
    link:
        - yass
        - mariage
environements:
    dev:
        expose:
            - "8080:80"